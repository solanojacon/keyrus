[project]
name = "bank-marketing-keyrus"
version = "0.1.0"
description = "Desafio técnico de modelagem em Python (Keyrus) — Bank Marketing dataset"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "kagglehub[pandas-datasets]>=1.0.2",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]
